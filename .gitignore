.idea
/scripts/results/
/results/
/resutls/
/.vscode/
/data/
/sdfs/
outputs/


# Convert checkpoint scripts (keep local but don't track in git)
scripts/convert_checkpoint.py
scripts/convert_megaflow_checkpoint.py

# Python cache files and directories
__pycache__/
*.py[cod]
*$py.class
.DS_Store

# Build artifacts
*.egg-info/

# Thermo labeling data artifacts (regenerable via data_processing scripts)
data_processing/batch*.log
data_processing/tcit_thermo_labels*
data_processing/neutralization_mapping.json
data_processing/chembl3d_neutralization_index.json*
data_processing/tcit_thermo.csv
data_processing/tcit_thermo.csv.stats.json

# Downstream FT data CSVs — raw / per-batch / superseded snapshots are
# large and regenerable; only the canonical audit output is tracked
# (currently downstream_ft/0511_cc_audit/).
downstream_ft/0301/
downstream_ft/0403/
downstream_ft/0506/
downstream_ft/0511/
# 0515_final is the dev mirror (lots of intermediate / iteration); the
# canonical 42-property snapshot lives at downstream_data/cv_0519/.
# Keep 0515_final locally for splitter iteration but stop tracking it.
downstream_ft/0515_final/

# Model checkpoints and large binaries
*.ckpt
*.jpt
*.pkl.gz
property_table.parquet

# PDFs and large assets
docs/*.pdf
assets/*.svg
assets/*.png

# Jupyter/IPython
.ipynb_checkpoints/
*.ipynb
